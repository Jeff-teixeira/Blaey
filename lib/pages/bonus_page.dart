import 'package:flutter/material.dart';

class BonusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bônus'),
      ),
      body: Center(
        child: Text('Conteúdo da Página de Bônus'),
      ),
    );
  }
}