import 'package:flutter/material.dart';

class TrophiesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Troféus e Ranking'),
      ),
      body: Center(
        child: Text('Página de Troféus e Ranking'),
      ),
    );
  }
}